## 整体结构

整个系统可以压缩成 6 个核心模块：

[前端UI]

↓ HTTP

[后端API层]

↓ 调用

[业务逻辑层]

↓ 调用

[CV核心模块]

↓

[数据库]

横向并行一个安全与异常处理模块

## 模块划分

如果有任何改动，都需要同步进文档对应部分（在你完成后）。

### 1. CV核心模块（主要功能实现）

放在独立文件夹下，文件夹命名如 cv_core/ 。CV模块不接触数据库、不接触HTTP，只取得输入图像，处理得到输出结果。

具体功能子模块拆分如下（不过大概还是一个人做）：

#### 1.1 人脸检测模块

输入：图片（numpy array）

输出：人脸框坐标列表（face_box）

detect_faces(image) -> [face_bbox]

依赖dlib库完成。

#### 1.2 Landmarks模块

输入：图片和人脸区域（face_box）

输出：68关键点（标准的人脸结构数字化 landmarks）

get_landmarks(image, face_bbox) -> landmarks

依赖dlib库完成。

#### 1.3 活体检测模块

输入：landmarks（需使用landmarks生成连续帧landmarks_sequence）

输出：是否活体（True/False）

liveness_check(landmarks_sequence, threshold, consec_frames, min_blinks) -> True/False

判断逻辑为计算EAR（眼睛纵横比），判断是否眨眼。若连续N帧检测人脸，检测到至少一次眨眼，则返回True。如果需要抗视频的话还需要添加别的功能（比如动作检测）。

依然使用dlib landmarks

#### 1.4 人脸识别模块

输入：人脸图像（在人脸检测模块上由原图切分出来的face_image = image[y:y+h, x:x+w]）

输出：身份ID or None

recognize_face(face_image, threshold) -> student_id

内部逻辑为编码->和人脸库比对。

在单独做内部逻辑模块时可以将人脸编码（128维向量）和对应身份存入一个临时的pickel文件，等到做到人脸数据库时再迁移过去。一个id允许有多个编码。

依赖库用 face_recognition（底层其实也是dlib封装）

#### 1.5 情绪识别模块

输入：人脸图像（face_image）

输出：情绪标签（emotion_label）

analyze_emotion(face_image, lang) -> emotion_label

推荐使用依赖DeepFace来进行直接的判断。

#### 1.6 合照识别模块（批处理）

输入：整张图片

输出：多个识别结果

recognize_group(image) -> [

{student_id, bbox}

]

注：情绪分析由 service 层在调用本模块后单独完成（调用 analyze_emotion），CV 模块不直接返回情绪。

**配套 CV 函数**：`recognize_face_topk(face_image, top_k, threshold)` — 返回前 `top_k` 个候选 `[(student_id, distance), ...]`，供 service 层贪心去重时使用。

### 1.7 CV 模块实现详解（答辩用）

#### 人脸检测：dlib HOG + 线性 SVM

**原理**：dlib 的 `get_frontal_face_detector()` 使用 HOG（Histogram of Oriented Gradients，方向梯度直方图）+ 线性 SVM（支持向量机）的经典组合。

- **HOG 特征提取**：将图像划分为小的 cell（如 8×8 像素），统计每个 cell 内像素的梯度方向和大小，生成梯度直方图。HOG 对光照变化不敏感，因为它关心的是边缘方向而非像素绝对值。
- **滑动窗口 + 图像金字塔**：检测器在不同尺度（通过图像金字塔实现）上滑动固定大小的窗口，对每个窗口提取 HOG 特征并用 SVM 判断是否为人脸。
- **NMS（非极大值抑制）**：多个重叠检测结果合并为一个最优结果，避免同一张脸被检测多次。

**为什么用 HOG 而不是深度学习检测器？**
- HOG 检测器内置于 dlib，无需额外下载模型文件
- 速度足够快（CPU 上几十毫秒），适合实时考勤场景
- 对正面人脸检测准确率高，课堂场景下学生基本正对摄像头

#### Landmarks：ERT 级联回归

**原理**：dlib 的 shape predictor 使用 ERT（Ensemble of Regression Trees）算法，将每个关键点的位置建模为一组回归树的级联。

- **初始化**：将平均人脸形状放置到检测框位置作为初始估计
- **迭代修正**：每级回归树根据当前 estimate 周围的像素强度特征，预测关键点位置的偏移量
- **级联结构**：多级回归树依次执行，每一步微调关键点位置，最终收敛到精确位置

68 点模型的点位分布用途：
- 0-16（下颌线）：勾勒脸型轮廓
- 17-26（眉毛）：表情分析
- 27-35（鼻子）：面部朝向判断
- 36-41（右眼）：EAR 眨眼检测的关键输入
- 42-47（左眼）：EAR 眨眼检测的关键输入
- 48-67（嘴唇+口腔）：表情和说话检测

#### 活体检测：EAR 眨眼检测

**原理**：EAR（Eye Aspect Ratio，眼睛纵横比）是一种几何特征，利用 6 个眼周关键点计算眼睛的开闭程度。

```
EAR = (|p2-p6| + |p3-p5|) / (2 × |p1-p4|)

其中 p1-p6 为单眼 6 个关键点：
  p2  p3
p1      p4  ← 眼角
  p6  p5
```

- **睁眼时**：垂直距离（分子）大，EAR ≈ 0.3~0.4
- **闭眼时**：垂直距离（分子）趋近 0，EAR < 0.2
- **眨眼判断**：EAR 从 >0.2 降到 <0.2 再升回 >0.2，视为一次完整眨眼

**为什么眨眼检测能防照片攻击？**
- 静态照片无法产生眨眼动作。连续帧的 EAR 值将保持恒定，不会出现下降-上升的模式
- 视频回放攻击更复杂，需要额外加入动作检测（如点头、转头），文档中已预留扩展空间

**阈值设计**（可调参数，在 `liveness.py` 中）：
- `EAR_THRESHOLD = 0.2`：闭眼判定阈值
- `CONSEC_FRAMES = 2`：连续 2 帧低于阈值才算闭眼，过滤传感器噪声
- `MIN_BLINKS = 1`：至少检测到 1 次眨眼

#### 人脸识别：128 维编码 + 欧氏距离

**原理**：`face_recognition` 库（底层为 dlib 的 ResNet-29 模型）将人脸图像映射为 128 维欧氏空间中的向量。

- **编码（Encoding）**：深度卷积网络提取人脸特征，输出 128 个浮点数。同一人的不同照片在空间中距离近，不同人的照片距离远。
- **比对（Recognition）**：计算查询人脸的 128 维向量与人脸库中所有向量的欧氏距离，距离最小且低于阈值（0.6）的即为匹配。

**为什么 128 维？**
- dlib 训练该模型时，在 LFW（Labeled Faces in the Wild）数据集上验证了 128 维在精度和效率间达到最优
- 每个向量仅 128×4=512 字节，1 万人的人脸库仅需约 5MB

**存储方案**：
- 临时方案：pickle 序列化 `{student_id: [encoding_128d, ...]}` 到 `data/encodings.pkl`
- 正式方案：将 encoding 序列化后存入 `face` 表的 `encoding BLOB` 字段，利用 SQLite 的索引加速查询

**注册与识别流程**：
```
注册：face_image → face_recognition.face_encodings() → 128d vector → 存入 encodings_db[student_id]
识别：face_image → 128d vector → 遍历 encodings_db 计算欧氏距离 → 取最小距离
     → 若 min_dist < 0.6 → 返回对应 student_id
     → 否则 → 返回 None（未知人脸）
```

**安全性考量**：
- 阈值 0.6 是经验值，可在特定场景下调至 0.5 以提高安全性（减少误识别）
- 一个 student_id 允许注册多张人脸编码，提高不同光照/角度下的识别率

#### 情绪识别：DeepFace 集成

**原理**：`DeepFace` 库封装了多种预训练深度学习模型，情绪识别使用的是在 FER+ 数据集上训练的卷积神经网络。

- **输入**：裁剪后的人脸图像（BGR → RGB 转换）
- **处理**：调用 `DeepFace.analyze(actions=['emotion'])`，自动进行人脸对齐和情绪分类
- **输出**：7 种情绪标签之一（angry, disgust, fear, happy, sad, surprise, neutral）
- **中文映射**：`emotion.py` 内建 `EMOTION_CN` 字典进行英-中翻译

**遇到的问题与解决**：
- **问题 1**：Windows GBK 编码下 DeepFace 的日志输出 emoji（如 🔗）导致 `UnicodeEncodeError`
- **解决**：在导入 DeepFace 之前通过 `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')` 重定向标准输出编码为 UTF-8
- **问题 2**：GitHub 不可达导致模型权重下载失败
- **解决**：通过 `ghproxy.net` 国内镜像下载 `facial_expression_model_weights.h5`（约 5.7MB）到 `~/.deepface/weights/`

### 2. 数据库模块（db/）

SQLite，零配置嵌入式数据库。

#### 2.1 ER 图

```
┌──────────────────┐         ┌──────────────────────────┐
│     student       │         │         face             │
├──────────────────┤         ├──────────────────────────┤
│ id        INTEGER │◄────────│ id          INTEGER       │
│ name      TEXT    │ 1    N  │ student_id  TEXT (FK)     │
│ student_id TEXT UN│         │ encoding    BLOB           │
└────┬─────────────┘         └──────────────────────────┘
     │
     │ 1 ──── N
     │
     ├──────────────────────────────────────────────┐
     │                                               │
     ▼                                               ▼
┌──────────────────────────┐  ┌──────────────────────────┐
│       attendance          │  │        emotion            │
├──────────────────────────┤  ├──────────────────────────┤
│ id        INTEGER         │  │ id        INTEGER         │
│ student_id TEXT (FK)      │  │ student_id TEXT (FK)      │
│ time      TEXT            │  │ time      TEXT            │
│ status    TEXT            │  │ emotion   TEXT            │
│ liveness  INTEGER         │  └──────────────────────────┘
│           0=failed         │
│           1=passed         │
│           2=suspicious     │
└──────────────────────────┘

┌──────────────────┐         ┌──────────────────────────┐
│    activity       │         │    activity_record        │
├──────────────────┤         ├──────────────────────────┤
│ id        INTEGER │◄────────│ id          INTEGER       │
│ name      TEXT    │ 1    N  │ student_id  TEXT (FK)     │
│ time      TEXT    │         │ activity_id INTEGER (FK)   │
└──────────────────┘         └──────────────────────────┘
```

**关系说明**

| 关系 | 类型 | 说明 |
|------|------|------|
| student → face | 1 : N | 一个学生可注册多张人脸编码 |
| student → attendance | 1 : N | 一个学生有多条考勤记录 |
| student → emotion | 1 : N | 一个学生有多条情绪记录 |
| student → activity_record | 1 : N | 一个学生参与多个活动 |
| activity → activity_record | 1 : N | 一个活动有多个学生参与 |
| activity_record | 多对多桥表 | 实现 student 与 activity 的多对多关系 |

#### 2.2 表结构详述

**student（学生表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 自增主键 |
| name | TEXT | NOT NULL | 姓名 |
| student_id | TEXT | NOT NULL UNIQUE | 学号，唯一标识 |

**face（人脸编码表）** — 预留给后续迁移使用（当前编码存储在 pickle 文件）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 自增主键 |
| student_id | TEXT | FOREIGN KEY → student.student_id | 关联学生 |
| encoding | BLOB | | pickle 序列化的 128 维向量 |

**attendance（考勤记录表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 自增主键 |
| student_id | TEXT | FOREIGN KEY → student.student_id | 关联学生 |
| time | TEXT | DEFAULT datetime('now','localtime') | 签到时间 |
| status | TEXT | DEFAULT 'present' | 出勤状态 |
| liveness | INTEGER | DEFAULT 0 | 活体/介质检测结果 |

**liveness 字段取值**（v0.6.0 扩展为三态）

| 值 | 常量名 | 含义 | 触发条件 |
|----|--------|------|---------|
| 0 | failed | 活体检测未通过 | 未检测到眨眼动作 |
| 1 | passed | 活体通过，非翻拍 | 眨眼通过 + 屏幕检测无异常 |
| 2 | suspicious_screen | 活体通过，疑似屏幕翻拍 | 眨眼通过 + Laplacian 方差 <100 阈值 |

**emotion（情绪记录表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 自增主键 |
| student_id | TEXT | FOREIGN KEY → student.student_id | 关联学生 |
| time | TEXT | DEFAULT datetime('now','localtime') | 记录时间 |
| emotion | TEXT | NOT NULL | 情绪标签（如 happy、neutral） |

**activity（活动表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 自增主键 |
| name | TEXT | NOT NULL | 活动名称 |
| time | TEXT | DEFAULT datetime('now','localtime') | 创建时间 |

**activity_record（活动参与记录表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 自增主键 |
| student_id | TEXT | FOREIGN KEY → student.student_id | 参与学生 |
| activity_id | INTEGER | FOREIGN KEY → activity.id | 活动 |

#### 2.3 输出接口（供业务层用）

| 函数 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `add_student(name, student_id)` | 姓名, 学号 | bool | 新增学生，重复返回 False |
| `get_student(student_id)` | 学号 | dict\|None | 查询单个学生 |
| `get_all_students()` | — | list[dict] | 全部学生列表 |
| `update_student_name(student_id, new_name)` | 学号, 新姓名 | bool | 编辑姓名 |
| `delete_student(student_id)` | 学号 | bool | 删除学生及关联记录（级联） |
| `save_attendance(student_id, status, liveness)` | 学号, 状态, 活体值 | int | 写入考勤，返回记录 ID |
| `query_attendance(date, student_id)` | 日期(可选), 学号(可选) | list[dict] | 查询考勤，含最近情绪 |
| `save_emotion(student_id, emotion)` | 学号, 情绪标签 | int | 写入情绪，返回记录 ID |
| `query_emotion(date, student_id)` | 日期(可选), 学号(可选) | list[dict] | 查询情绪记录 |
| `get_emotion_stats(date, student_id)` | 日期(可选), 学号(可选) | list[dict] | 情绪 GROUP BY 统计 |
| `create_activity(name)` | 活动名 | int | 创建活动，返回 ID |
| `add_activity_record(student_id, activity_id)` | 学号, 活动ID | bool | 添加参与记录 |
| `query_activity(activity_id, date, name, date_start, date_end)` | 活动ID/日期/名称/时间段(均可选) | list[dict] | 查询活动及参与详情 |
| `delete_activity(activity_id)` | 活动ID | bool | 删除活动及参与记录 |
| `get_activity_freq_stats(activity_ids)` | 活动ID列表(空=全部) | list[dict] | 学生参与频次统计，按次数降序 |
| `get_merged_activity_participants(activity_ids)` | 活动ID列表 | list[dict] | 多活动参与人员明细，供合并导出 |
| `get_activity_stats()` | — | dict | 活动统计概览 |
| `export_attendance_raw(date, student_id)` | 日期(可选), 学号(可选) | list[dict] | 导出用，与 query 同源 |

#### 2.3 实现详解（答辩用）

**技术选型：SQLite**

选择 SQLite 而非 MySQL 的理由：
- **零配置**：SQLite 是 Python 标准库自带，不需要单独安装数据库服务。答辩演示时直接 `python app.py` 就能跑，评委不用配环境。
- **嵌入式**：数据库就是一个 `.db` 文件，放在 `data/` 目录下，复制项目即复制数据。
- **并发够用**：课堂场景下并发量极低（单台机器、单人操作），SQLite 完全胜任。WAL 模式（Write-Ahead Logging）允许并发读取不被写入阻塞。

**表设计思路**

6 张表遵循第三范式（3NF），避免数据冗余：
- `student` 和 `face` 拆分：一个学生可以注册多张人脸照片（不同角度/光照），产生多条 encoding 记录，用 `student_id` 外键关联。
- `attendance` 和 `emotion` 独立：考勤记录和情绪记录是不同的业务实体，分开存储便于独立查询和统计。
- `activity` 和 `activity_record` 是多对多关系：一个活动有多个学生参与，一个学生可以参与多个活动，通过中间表 `activity_record` 关联。

**关键实现技术**

| 技术点 | 说明 |
|--------|------|
| `sqlite3.Row` | 将查询结果设为 `sqlite3.Row` 对象，支持 `row["name"]` 字典式访问，比元组索引更可读 |
| WAL 模式 | `PRAGMA journal_mode = WAL`，写操作不阻塞读操作，后续扩展为 Web 服务时避免数据库锁冲突 |
| 外键约束 | `PRAGMA foreign_keys = ON`，删除学生时级联删除关联的人脸编码和记录，保证数据一致性 |
| 事务管理器 | `@contextmanager` + `with` 语句，自动处理 commit/rollback，避免手动管理事务的遗漏 |
| 参数化查询 | 全部使用 `?` 占位符传参，从根本上杜绝 SQL 注入。即使输入恶意字符串也无法改变 SQL 语义 |
| 查询/导出复用 | `export_attendance_raw()` 直接调用 `query_attendance()`，查询 SQL 只写一份，保证导出和查询数据一致 |

**参数化查询安全性示例**

```python
# 正确做法（我们的实现）：
conn.execute("SELECT * FROM student WHERE student_id = ?", (user_input,))
# SQL 引擎将 user_input 当作纯字符串值处理，不会解析为 SQL 命令

# 错误做法（绝对避免）：
conn.execute(f"SELECT * FROM student WHERE student_id = '{user_input}'")
# 如果 user_input = "1' OR '1'='1"，则变成 SELECT * FROM student WHERE student_id = '1' OR '1'='1'
# 这会查出所有学生，造成数据泄露
```

**查询条件动态拼接**

`query_attendance(date, student_id)` 使用 `WHERE 1=1` 技巧：
- 基础 SQL 始终有效
- 有 date 条件时追加 `AND date(a.time) = ?`
- 有 student_id 条件时追加 `AND a.student_id = ?`
- 两个参数都传则两项都追加，都不传则查全部
- 避免了复杂的 `if-elif-else` 分支判断

### 3. 业务逻辑层（service/）

这部分本质是用Python 本身将模块拆分，将具体能力函数组织为业务流程和宏观功能。

如果针对实际情况做出了改动，更改也要同步进文档里。

#### 3.1 考勤服务

业务流程：

process_attendance(images):
faces = detect_faces(最后一帧)
→ 选最大人脸
→ 每帧 landmarks 提取
→ liveness_check(landmarks_sequence)
→ recognize_face(主帧人脸)
→ screen_attack_detect(主帧人脸)
→ 写入数据库
→ 返回结果（code/msg/data 统一格式）

返回结果 data：

{

name,

student_id,

status,

emotion,

liveness,           # "passed" / "failed" / "suspicious_screen"

liveness_val,       # 0 / 1 / 2

screen_score,       # 屏幕翻拍可疑度

screen_moire_pr,    # FFT 峰值比

screen_lap_var,     # Laplacian 方差

frames_collected    # 采集到的 landmarks 帧数

}

#### 3.2 合照服务

process_group(image, activity_name):

faces = detect_faces

→ 每个 face:

recognize_face_topk(face, top_k=3) + analyze_emotion

→ 贪心去重 + 二次分配（_deduplicate_assignments）

→ 写入活动记录（activity + activity_record + emotion）

→ 返回 {activity_id, activity_name, total_faces, identified, results[{student_id, name, bbox, emotion}]}

activity_name 为空时默认使用当天日期（如 20260511）。去重逻辑确保同一个人不会被分配到合照中的多张不同人脸。每次合照创建一条 activity 记录，识别到的每个学生写入一条 activity_record。

#### 3.3 查询服务

get_attendance(date, student_id)

get_emotion(date, student_id)

get_activity(activity_id, date, name, date_start, date_end)

get_student_list()

导出功能：
export_attendance(date, student_id, fmt):
→ 调 query_attendance() → pandas DataFrame → Excel/CSV

export_activity(date, name, fmt):
→ 调 query_activity() → pandas DataFrame → Excel/CSV

注意保持查询和导出要服用一套SQL逻辑，导出 = 查询结果 + 格式转换。

### 3.4 实现详解（答辩用）

**分层架构设计**

业务逻辑层是 CV 核心模块和数据库模块之间的桥梁。设计原则：
- **CV 模块不接触数据**：人脸检测/识别/情绪分析函数只处理 `np.ndarray`，不感知 SQLite 的存在
- **db 模块不接触 CV**：CRUD 函数只处理数据存取，不知道数据是怎么来的
- **service 层负责编排**：串联上述两个模块，定义业务流程

这种分层的好处：修改 CV 算法或替换数据库不会相互影响。

**考勤服务流程（完整调用链）**

```
process_attendance(image)
  1. detect_faces(image)              → 获取所有人脸位置
  2. get_largest_face()              → 取最大人脸（考勤假设单人场景）
  3. get_landmarks(image, bbox)      → 68 关键点
  4. liveness_check(landmarks_seq)   → EAR 眨眼判断
  5. recognize_face(face_img)        → 128d 编码比对
  6. analyze_emotion(face_img)       → DeepFace 情绪分类
  7. save_attendance(student_id)     → 写入 attendance 表
  8. save_emotion(student_id)        → 写入 emotion 表
  9. 返回统一格式 {code, msg, data}
```

**合照服务 vs 考勤服务**

| 对比维度 | 考勤服务 | 合照服务 |
|----------|---------|---------|
| 人脸处理 | 选最大人脸（单人） | 遍历所有人脸（多人） |
| 活体检测 | 需要（防照片攻击） | 不需要（合照通常不是实时采集） |
| 数据库写入 | attendance + emotion 两张表 | 创建 activity + 写入 activity_record |
| 返回数据 | 单人结果 | 多人列表 + 统计 |

**查询/导出复用设计**

```python
# 查询：返回 dict 列表给前端展示
def get_attendance(date, student_id):
    rows = db.query_attendance(date, student_id)  # 同一 SQL
    return {"code": 0, "data": {"records": rows}}

# 导出：同一 SQL + 格式转换
def export_attendance(date, student_id, fmt):
    rows = db.query_attendance(date, student_id)  # 同一 SQL
    df = pd.DataFrame(rows)
    # 中文列名映射: id→序号, student_id→学号, name→姓名...
    df.to_excel(filepath)  # 或 df.to_csv(filepath)
    return filepath
```

保证查询页面看到的数据和导出的 Excel/CSV 完全一致——同一套 SQL，不存在「页面上显示 10 条，导出变成 8 条」的 bug。

### 5.1 前端实现详解（答辩用）

### 4. 后端API层（Flask）

注意每个接口对应一个功能，不要混淆。同样，如果有任何需要的改动，都要同步进文档。

#### 4.1 api设计

考勤

POST /api/attendance

input: frames（多帧图片列表，兼容单帧 image）

output: {code, msg, data: {name, student_id, status, emotion, liveness, screen_score, ...}}

合照

POST /api/group

input: image + activity_name（可选，默认当天日期）

output: {activity_id, activity_name, total_faces, identified, results[{student_id, name, bbox, emotion}]}

合照活动管理

GET /api/activity?date_start=xxx&date_end=xxx&name=xxx  （时间段+模糊搜索活动列表）

GET /api/activity/<id>/participants   （查看活动参与人员，含情绪）

DELETE /api/activity/<id>   （删除活动及参与记录）

POST /api/activity/freq-stats   {activity_ids: []}   （参与频次统计，空数组=全部活动）

POST /api/activity/merged-export   {activity_ids: [...], format: "excel"|"csv"}   （合并导出）

查询/导出

GET /api/attendance?date=xxx

GET /api/emotion

GET /api/activity

GET /api/export?date=xxx&student_id=xxx

人脸库管理

POST /api/register

PUT /api/student

DELETE /api/student

GET /api/students

其他

GET /api/ping

#### 4.2 api返回格式

所有API统一返回格式：

{

code: 0,

msg: "ok",

data: {...}

}

### 4.3 实现详解（答辩用）

**Flask 框架选型**

选择 Flask 而非 Django/FastAPI 的理由：
- **轻量**：项目只有 7 个 API 端点，不需要 Django 的全套 ORM/Admin/Template。Flask 的微内核设计刚好匹配项目规模。
- **蓝图（Blueprint）**：将路由集中定义在 `api/routes.py` 中，通过 `Blueprint("api", url_prefix="/api")` 注册到 app，实现路由与应用的解耦。
- **CORS**：`flask-cors` 启用跨域访问，前端 HTML 页面（file:// 或不同端口）可以调用后端 API。

**统一返回格式**

所有接口返回 `{"code": 0, "msg": "ok", "data": {...}}`：
- `code: 0` — 成功，非 0 — 异常（对应安全与异常处理表格的错误码）
- `msg` — 人类可读的描述信息
- `data` — 实际载荷，异常时为 `null`

前端只需检查 `code == 0` 即可判断成功与否，不必在每个请求处写不同的错误处理逻辑。

**图片上传处理**

```
前端 FormData(image=文件) → Flask request.files["image"]
  → np.frombuffer(file.read()) → cv2.imdecode → BGR numpy array
```

- 使用 `multipart/form-data` 传输，避免 base64 编码开销（图片直传二进制）
- 后端用 `ALLOWED_EXTENSIONS = {"png", "jpg", "jpeg", "bmp", "webp"}` 做文件类型白名单校验
- 解码后的 BGR 数组直接传入 CV 核心模块，不落盘

**端点设计**

| 端点 | 方法 | 功能 | 异常码 |
|------|------|------|--------|
| `/api/ping` | GET | 健康检查 | — |
| `/api/register` | POST | 人脸注册（人脸检测→编码→保存） | 1(无效图片) 2(无人脸) 3(编码失败) |
| `/api/attendance` | POST | 考勤签到（完整流程） | 1(无效图片) 2(无人脸) 4(活体失败) 5(未识别) |
| `/api/group` | POST | 合照识别 | 1(无效图片) 2(无人脸) |
| `/api/attendance` | GET | 考勤记录查询（?date=&student_id=） | — |
| `/api/emotion` | GET | 情绪记录查询+统计 | — |
| `/api/activity` | GET | 活动及参与查询 | — |
| `/api/students` | GET | 学生列表 | — |
| `/api/export` | GET | 导出 Excel/CSV（?format=） | — |
| `/api/student` | DELETE | 删除学生（?student_id=） | 1(学号为空) |

**端点复用**：`/api/attendance` 用 HTTP 方法区分功能——POST 是签到操作，GET 是查询操作。同一 URL、不同语义，符合 RESTful 惯例。

**人脸库管理接口**

`POST /api/register` 的完整流程：
```
前端上传图片 + 学号 + 姓名
  → 后端检测人脸 (detect_faces)
  → 取最大人脸区域
  → 调用 register_face() 生成 128d 编码并存入内存
  → 调用 save_encodings() 持久化到 data/encodings.pkl
  → 调用 add_student() 写入 student 表
  → 返回 {student_id, name}
```

`DELETE /api/student` 的级联清理：
```
前端发送 DELETE /api/student?student_id=xxx
  → 从内存编码库删除该学生的所有编码
  → 持久化编码库
  → 从数据库删除 student 记录（外键级联删除 face/attendance/emotion/activity_record）
```

### 5.前端

技术不确定，实在不行就用HTML + CSS + JS。要用vue的话我不会，能搞出来能用的也行。

页面功能拆分如下：

#### 5.1 考勤页面

需要实现：摄像头调用预览、手动拍摄拍照按钮、采集状态显示、结果显示（姓名、状态、情绪、活体、屏幕检测）。

#### 5.2 合照页面

功能：

- 上传合照（支持拖拽/点击，预览居中显示，悬停可 × 关闭重新选择）
- 活动名称输入（默认当天日期，如 20260511）
- 识别结果卡片（可折叠，含统计概览 + 参与者表格 + 情绪列）
- 识别记录面板（可折叠，左右分栏）

识别记录面板内：
- **左侧 55%**：活动列表，每页 10 条，支持时间段搜索 + 名称模糊搜索，每条前有勾选框（表头全选），翻页栏固定在卡片底部
- **右侧 45%**：参与频次统计，水平柱状图（Chart.js）+ 排名表，勾选活动后实时联动更新（全不勾选 = 统计全部）
- 合并导出：勾选若干活动后导出 Excel/CSV，含活动名称、时间、学号、姓名
- 每条活动可查看参与人员弹窗（含情绪，表头固定可滚动）、删除（二次确认）

#### 5.3 数据页面

功能：

查询部分

功能包含日期选择（date picker）、学号输入框、查询按钮

支持只选日期（查某天全班）、只填学号（查某人全部）、两个一起（查某人某天）

考勤记录图表

包含情绪统计（柱状图或饼状图）、出勤统计、导出按钮（导出为excel、csv）

两部分可以分上下展示，上部为查询部分、下部为展示部分，展示部分无查询时默认查询全部的统计。

### 5.1 前端实现详解（答辩用）

**技术选型：原生 HTML + CSS + JS + Chart.js CDN**

不引入前端框架（Vue/React）的理由：
- 教师未强制要求前端框架，原生方案足够
- 三个功能页面，组件复用需求低
- 零构建步骤，修改即刷新，答辩现场改参数方便

**页面架构**

三个 HTML 页面共享：
- 同一个 CSS 文件（`style.css`，CSS 变量 + 响应式布局）
- 同一个导航栏（sticky 顶部，当前页高亮）
- 统一的 Toast 提示组件（右上角滑入，2.5s 自动消失）

**考勤页面（index.html）核心技术**

| 技术点 | 实现 |
|--------|------|
| 摄像头调用 | `navigator.mediaDevices.getUserMedia({video:...})` — 浏览器原生 API |
| 图像采集 | `<video>` 实时预览 → `canvas.drawImage()` 截帧 → `canvas.toBlob()` 生成 JPEG |
| 图片上传 | `FormData.append('image', blob)` → `fetch POST /api/attendance`，二进制直传 |
| 图像采集 | `canvas.drawImage()` 截帧 → `canvas.toBlob()` 生成 JPEG，每次签到采集 8 帧（120ms 间隔） |
| 结果展示 | 统一返回格式，检查 `code==0` 判断成功/失败，分别渲染绿/红提示卡片 |

**合照页面（group.html）核心技术**

| 技术点 | 实现 |
|--------|------|
| 文件选择 | `<input type="file" hidden>` + 点击区域触发 + 拖拽事件（`dragover/drop`） |
| 图片预览 | `FileReader.readAsDataURL()` 本地预览，不上传前不消耗带宽 |
| 结果表格 | 识别结果以 `<table>` 展示，统计卡片以 CSS Grid `stats-grid` 展示 |

**数据页面（data.html）核心技术**

| 技术点 | 实现 |
|--------|------|
| 图表库 | Chart.js v4 CDN（`cdn.jsdelivr.net`），饼图展示情绪分布 |
| 并行请求 | `Promise.all([fetch('/api/attendance'), fetch('/api/emotion'), fetch('/api/students')])` |
| 查询条件 | URL query string 拼接，`?date=xxx&student_id=xxx`，支持空值（查全部） |
| 导出 | `<a download>` 触发浏览器下载 Blob（Excel/CSV），无需额外库 |

**Chart.js 情绪饼图**

- `type: 'pie'` — 饼图直观展示各类情绪占比
- 7 种情绪映射 7 种颜色（红→愤怒、绿→高兴、灰→中性等）
- 响应式：`responsive: true`，图例在底部，适配手机/投影仪

**异常处理前端侧**

| 异常 | 前端处理 |
|------|---------|
| 摄像头不可用 | `getUserMedia` reject → 显示"摄像头不可用: <错误信息>" |
| 图片格式非法 | 前端 `accept` 属性 + 文件类型检测，不支持的格式不发送 |
| 请求超时 | `fetch` 失败 → 显示"网络错误" |
| 后端返回错误码 | 检查 `code != 0` → 用后端 `msg` 渲染错误提示卡片 |

### 6.安全与异常模块（贯穿）

要覆盖：摄像头失败、图片上传失败、无人脸、多人脸（考勤时）、超时。对于每种情况要给出清晰对应的报错信息，对于每一种情况应作出预留的default处理。

## 模块调用关系

核心调用链：

考勤：

前端

→ /api/attendance

→ service.attendance

→ cv_core

→ db

→ 返回JSON

→ 前端渲染

合照识别：

前端 → /api/group → service.group → cv_core (detect + recognize + emotion) → db → JSON → 前端渲染

合照查询/统计：

前端 → /api/activity → service.query → db (聚合+筛选) → JSON

前端 → /api/activity/freq-stats → db (GROUP BY 频次统计) → JSON → Chart.js 柱状图

合照合并导出：

前端 → /api/activity/merged-export → db (多活动合并查询) → Excel/CSV

查询/导出：

前端 → /api/attendance(query) → service → db

前端 → /api/export → service → db → Excel

可能疏漏了别的功能，如果要用上的话记得往里写

## 附：提示词模板

你正在开发一个模块：

模块名：face_detection.py

输入：

- image (numpy array)

输出：

- list of face bounding boxes

要求：

- 使用OpenCV或dlib

- 不涉及数据库

- 不涉及HTTP

- 提供清晰函数接口

请只生成这个模块代码，并解释函数结构

---

## 附：开发环境激活

### conda 环境
项目使用 conda 虚拟环境 `checkin`（Python 3.9），conda 安装路径 `D:\softwares\anaconda3`。

**Git Bash（推荐）**
```bash
source /d/softwares/anaconda3/etc/profile.d/conda.sh && conda activate checkin && cd D:/ccworks/homework/CheckIn-contentsec
```

**Windows cmd**
```cmd
D:\softwares\anaconda3\Scripts\activate.bat checkin
cd /d D:\ccworks\homework\CheckIn-contentsec
```

**Windows PowerShell**
```powershell
& D:\softwares\anaconda3\Scripts\activate.ps1 checkin
cd D:\ccworks\homework\CheckIn-contentsec
```

### 依赖安装
```bash
pip install -r requirements.txt
```

### CV 模块独立验证命令
```bash
python cv_core/face_detection.py <图片路径>     # 人脸检测
python cv_core/landmarks.py <图片路径>          # 68关键点
python cv_core/liveness.py <图片路径>           # EAR计算（活体需摄像头）
python cv_core/recognizer.py register <图片> <学号>   # 注册人脸
python cv_core/recognizer.py recognize <图片>         # 识别人脸
python cv_core/emotion.py <图片路径>            # 情绪识别
python cv_core/group.py <图片路径>              # 合照识别
python cv_core/screen_attack_detect.py <图片路径>  # 屏幕翻拍检测
```

---

## 附：开发迭代记录

以下记录开发过程中与原始设计文档的差异及演进决策。

### 人脸注册功能

- **注册弹窗**：从导航栏独立按钮改为所有页面右上角固定浮层入口（`btn-fixed-register`），点击弹出 Modal 弹窗，内含独立摄像头预览，支持拍照注册和本地上传两种方式。
- **注册弹窗摄像头**：弹窗内嵌 `<video>` 元素，打开时自动获取摄像头流，关闭时释放。与签到页共用同一摄像头设备。
- **防重复注册**：`POST /api/register` 先查 `get_student()` 是否存在，再插入数据库。重复注册返回 `code=6` 并提示已注册姓名。
- **注册流程顺序**：先写数据库 (`add_student`) → 再持久化编码 (`save_encodings`)，防止数据库失败但编码已存的脏状态。

### 学生管理

- **学生列表弹窗**：数据查询页「注册学生」统计卡片可点击，弹出 Modal 显示全部已注册学生。
- **搜索过滤**：弹窗顶部搜索框支持按学号或姓名的实时过滤。
- **翻页**：每页 10 条，底部有第一页/上一页/页码/下一页/末页按钮。
- **编辑姓名**：`PUT /api/student` 接口，弹窗内点击“编辑”按钮进入行内编辑模式，修改后点“保存”。
- **删除学生**：`DELETE /api/student?student_id=xxx`，删除时级联清除其人脸编码、考勤记录、情绪记录和活动参与记录。前端有确认弹窗。

### 活体检测调优

- **ORIGINAL 阈值**：`EAR_THRESHOLD=0.20`, `EAR_CONSEC_FRAMES=2`
- **ADJUSTED 阈值**：`EAR_THRESHOLD=0.18`, `EAR_CONSEC_FRAMES=1`
- **前端采集参数**：12 帧 × 80ms 间隔 = ~1 秒（原 10 帧 × 200ms）
- **原因**：原始参数在 200ms 帧间隔下容易漏掉快速眨眼（眨眼闭眼相仅 50-100ms）。

### 性能优化

- **图片缩放**：`service/attendance.py` 强制将所有帧缩放至宽度 ≤640px，保持宽高比。dlib HOG 检测器在大图上极慢（6560×2952 需 8s/帧），缩放后约 150ms/帧。
- **异步情绪分析**：`DeepFace.analyze()` 调用耗时 5-15s，改为后台线程执行，不阻塞签到响应。签到结果中情绪字段显示“分析中...”，后台完成后写入 `emotion` 表。
- **Debug 模式关闭**：Flask `debug=False`，避免自动 reloader 在数据库写入时触发重载导致请求中断。

### 前端交互优化

- **考勤页双栏布局**：左侧摄像头 + 右侧签到结果，同屏显示。窄屏自动堆叠。
- **emoji 清理**：考勤状态和签到结果去掉了 ✅/❌ emoji，改用 CSS 颜色类（`.text-success` / `.text-danger`）。
- **查询缓存修复**：数据查询页的 `fetch` 请求添加 `cache: no-store` 和时间戳参数，避免浏览器缓存导致清空条件后仍显示上次结果。
- **考勤记录情绪列**：`query_attendance` SQL 通过子查询关联最近的 `emotion` 记录，前端表格增加“情绪”列，英文标签映射为中文。

### 代码组织

- **共享 JS**：注册弹窗逻辑抽取为 `frontend/js/register-modal.js`，三个页面均引用，避免重复代码。
- **API_BASE 冲突修复**：`register-modal.js` 不再声明 `const API_BASE`，复用各页面主 JS 中的声明。

### 屏幕翻拍攻击检测（screen_attack_detect.py）

#### 模块定位

`cv_core/screen_attack_detect.py` 是新增的**介质检测**模块，与活体动作检测（眨眼 EAR）属于不同层面：

| 对比维度 | 眨眼活体检测（liveness.py） | 屏幕翻拍检测（screen_attack_detect.py） |
|----------|---------------------------|----------------------------------------|
| 检测对象 | 是否活人（有眨眼动作） | 是否是屏幕播放的视频 |
| 检测方式 | 连续帧 EAR 时序分析 | 单帧图像质量分析 |
| 防御目标 | 静态照片攻击 | 动态视频回放攻击 |
| 依赖 | landmarks 序列 | 人脸区域像素 |

#### 考勤流程集成顺序

```
人脸检测 → landmarks 提取 → 眨眼活体检测（liveness.py）
  ├─ 眨眼失败 → 拒绝 (code=4, liveness=failed)
  └─ 眨眼通过 → 人脸识别
       ├─ 识别失败 → code=5
       └─ 识别成功 → 屏幕翻拍检测（screen_attack_detect.py）
            ├─ 无异常 → liveness=passed (值1)
            └─ 疑似翻拍 → liveness=suspicious_screen (值2)
                         仍允许签到，msg 提示"疑似屏幕翻拍，已标记"
```

此设计遵循"硬阻断 + 软标记"策略：眨眼失败是硬阻断（直接拒绝），屏幕检测是软标记（允许签到但标记可疑状态），避免误判影响正常签到体验。

#### 特征选择与校准历程

**初始方案（理论设计）**

- 特征 1：FFT 摩尔纹检测（屏幕像素网格与摄像头传感器干涉产生周期性条纹）
- 特征 2：Laplacian 模糊度（二次成像经历镜头+屏幕+镜头的信息损失）
- 特征 3：HSV 色偏分析（屏幕发光光谱 vs 自然反射光差异）
- 特征 4：镜面反射异常（屏幕镜面反射 vs 人脸漫反射）
- 权重：FFT 主（0.45），Laplacian 辅（0.25），HSV（0.20），高光（0.10）

**第一轮实测（真人×6 vs 视频回放×4）**

在用户设备上使用同一摄像头分别采集真人签到和手机播放眨眼视频签到，结果如下：

| 维度 | 真人直拍（n=6） | 视频翻拍（n=4） | 区分力 |
|------|---------------|----------------|--------|
| FFT 峰值比 | 2.33 ~ 2.96 | 2.32 ~ 2.65 | ✗ 完全重叠，无区分力 |
| Laplacian 方差 | 107 ~ 278 | 42 ~ 95 | ★ 干净分离，阈值 100 |

**关键发现**：FFT 摩尔纹在真人和翻拍图像上均有较强响应（峰值比均 > 2.0），这是因为摄像头 CMOS 传感器本身存在固定模式噪声（Fixed Pattern Noise）以及 JPEG 压缩引入的块效应伪影，并非只有屏幕翻拍才会产生频域峰值。因此 FFT 摩尔纹在普通消费级 RGB 摄像头上**不适合作为翻拍检测的主特征**。

Laplacian 方差则表现出稳定区分力：屏幕翻拍因经历"镜头 1 → 屏幕显示 → 镜头 2"的二次成像过程，高频细节（皮肤纹理、毛孔、睫毛等）被显著衰减，方差值始终低于真人直拍。

**第二轮校准（基于第二轮数据：真人×4 + 视频×4）**

根据实测数据调整策略，结果不受影响，分离依然干净。最终阈值如下：

| 参数 | 校准值 | 依据 |
|------|--------|------|
| `FFT_PEAK_RATIO_THRESHOLD` | 2.9 | 仅极高值才触发，真人最高 2.96，阈值 2.9 使其极少误触 |
| `LAP_VAR_MIN` | 100.0 | 真人最低 107，视频最高 97，阈值 100 完美分隔 |
| `W_BLUR`（Laplacian 权重） | 0.9 | Laplacian 是主特征，权重高度倾斜 |
| `W_FFT`（FFT 权重） | 0.1 | FFT 降级为弱辅助，仅极端情况下加分 |
| `SCORE_THRESHOLD` | 0.5 | 综合可疑度 ≥ 0.5 判定为 suspicious_screen |

**最终判定逻辑**

- 真人直拍：Laplacian > 100（不触发）→ score ≈ 0.0 → **通过（liveness=passed）**
- 视频翻拍：Laplacian < 100（触发）→ score ≈ 0.9 → **标记可疑（liveness=suspicious_screen）**
- 极端摩尔纹：即使 Laplacian 正常，若 FFT 峰值比 > 2.9 → score ≈ 0.1 → **仍通过**

#### 数据库变更

`attendance` 表的 `liveness` 字段从布尔（0/1）扩展为整型三态：

| 值 | 含义 | 对应状态 |
|----|------|---------|
| 0 | failed | 眨眼活体检测未通过 |
| 1 | passed | 活体检测通过 + 屏幕检测无异常 |
| 2 | suspicious_screen | 活体通过但屏幕检测疑似翻拍 |

#### 性能验证

经过图片缩放优化（所有帧统一缩放到宽度 ≤ 640px），`detect_screen_attack()` 的单次调用耗时 < 50ms，不影响签到响应速度。

#### 已知局限性

1. **光照敏感**：暗光环境下真人直拍的 Laplacian 方差可能下降，接近翻拍范围。建议实现在考勤页面提示用户保持充足光照。
2. **高分辨率屏幕**：4K/Retina 屏幕播放的视频，像素密度极高可能导致二次成像的模糊度降低，Laplacian 方差升高。
3. **单帧检测**：仅对识别主帧做检测，若攻击者在该帧恰好移开手机可能漏检。
4. **设备差异**：不同摄像头的固有噪声和锐化程度不同，阈值可能需要在其他设备上重新校准。

#### 数据库查询修复

屏幕翻拍检测引入后，`query_attendance` 的 SQL 子查询（关联 emotion 表取最近情绪记录）使用了外层别名 `a.time` 作为 `abs(strftime(...))` 的参数。该写法在 SQLite 部分版本和配置下不稳定，导致 `GET /api/attendance` 持续返回 500 错误（`no such column: a.time`），数据查询页面完全瘫痪。

**修复方法**：将情绪关联从纯 SQL 子查询改为 Python 端后处理——先查 attendance + student 的 LEFT JOIN，再对每条记录用独立查询取最近情绪。SQL 兼容性问题消除，接口恢复正常。

#### 情绪饼图筛选联动修复

数据查询页的情绪饼图在选择学号或日期筛选后仍显示全量统计——根因是 `get_emotion_stats()` 函数签名缺少 `student_id` 参数，导致 `service/query.py` 调用时无法下传筛选条件，GROUP BY 始终对全表聚合。修复后 `get_emotion_stats(date, student_id)` 支持双重筛选，饼图与考勤表格保持数据一致。

### 合照模块改造：活动单元模式

#### 功能概述

将合照识别从"单次识别展示"升级为以活动为核心的完整管理模块，包含创建、查询、统计、导出全流程。

#### 页面布局

```
┌──────────────────────────────────────────────────┐
│  上传合照                                        │
│  ┌──────────────────────────────────────────┐    │
│  │         [点击选择 / 拖拽合照]              │    │
│  │         × 可关闭预览                      │    │
│  └──────────────────────────────────────────┘    │
│  活动名称: [20260511________]  [开始识别]        │
├──────────────────────────────────────────────────┤
│  识别结果 ▴  (可折叠)                             │
│  ┌──────────────────────────────────────────┐    │
│  │ 统计卡片 | 参与者表格（含情绪列）          │    │
│  └──────────────────────────────────────────┘    │
├──────────────────────────────────────────────────┤
│  识别记录 ▾  (可折叠)                             │
│  时间段搜索: [起始日期] [截止日期] [名称] [查询]  │
│  ┌─────────────┬───────────────────────────┐    │
│  │ 活动列表     │  参与频次统计              │    │
│  │ (55% 宽)    │  (45% 宽)                 │    │
│  │             │                           │    │
│  │ ☑ 活动名    │  ┌── 柱状图 ──────────┐   │    │
│  │ ☐ 活动名    │  │ 学生A ████████ 8次 │   │    │
│  │ ☐ 活动名    │  │ 学生B ████     4次 │   │    │
│  │             │  └────────────────────┘   │    │
│  │ [翻页栏]    │  排名表 (学号/姓名/次数)    │    │
│  └─────────────┴───────────────────────────┘    │
│  [合并导出 Excel] [合并导出 CSV]                  │
└──────────────────────────────────────────────────┘
```

#### 活动名称管理

- 新建合照时，活动名称默认填入当天日期（格式如 `20260511`），可手动修改
- 识别后以该名称在 `activity` 表中创建活动记录

#### 识别记录查询

| 功能 | 实现 |
|------|------|
| 时间段搜索 | `date_start` / `date_end` 参数，BETWEEN 查询 |
| 名称模糊搜索 | `name LIKE %keyword%` |
| 每页 10 条 | 前端分页，翻页栏固定于卡片底部 |
| 排序 | 从新到旧（最新活动在最上面） |

#### 识别记录操作

| 操作 | 说明 |
|------|------|
| 查看 | 弹窗显示参与人员列表（学号/姓名/情绪），560px 宽，超出 60vh 可滚动，表头固定 |
| 删除 | 二次确认弹窗，级联删除活动及参与记录 |
| 合并导出 | 勾选多个活动 → 合并所有参与人员到一个 Excel/CSV，含活动名、时间、学号、姓名 |
| 全选 | 表头复选框可一键全选/取消 |

#### 参与频次统计

- **实时联动**：勾选活动后右侧柱状图 + 排名表立即更新；全不勾选 = 统计全部活动
- **柱状图**：水平条形图（Chart.js），按参与次数降序，最多显示前 15 人
- **排名表**：学号 / 姓名 / 参与次数，按次数降序
- **后端 API**：`POST /api/activity/freq-stats`，接收 `activity_ids` 数组，空数组 = 全部活动
- **数据库查询**：`get_activity_freq_stats(activity_ids)`，动态拼接 `IN (?)` 子句

#### 新增后端接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/activity?date_start=&date_end=&name=` | GET | 活动列表查询，支持时间段和模糊搜索 |
| `/api/activity/<id>/participants` | GET | 获取活动参与人员详情（含情绪） |
| `/api/activity/<id>` | DELETE | 删除活动及参与记录 |
| `/api/activity/freq-stats` | POST | 参与频次统计，`{"activity_ids": [...]}` |
| `/api/activity/merged-export` | POST | 合并导出，`{"activity_ids": [...], "format": "excel|csv"}` |

#### 新增数据库函数

| 函数 | 说明 |
|------|------|
| `query_activity(date_start, date_end)` | 活动查询，新增时间段参数 |
| `delete_activity(activity_id)` | 删除活动及参与记录 |
| `get_activity_freq_stats(activity_ids)` | 按学生统计参与频次 |
| `get_merged_activity_participants(activity_ids)` | 获取指定活动的全部参与明细 |

#### 合照识别增加情绪分析

`process_group()` 在识别每张人脸后调用 `analyze_emotion()`，情绪结果随识别结果返回，同时写入 `emotion` 表。参与人员弹窗中新增情绪列（中文标签）。

#### 合照去重：贪心 + 二次分配

**问题**：独立匹配下，合照中多张不同人脸可能被识别为同一个人，但同一人不可能同时出现在同一张照片的两处。

**方案**：
1. `recognize_face_topk(face, top_k=3)` — 每张人脸返回前 3 个候选 ID 及其距离
2. 贪心去重 — 收集所有人脸的所有候选，按距离升序排列，依次分配。距离越小的匹配越可信，优先保留。已占用的 ID 不再分配给后续人脸
3. 二次分配 — 冲突人脸从剩余候选（排除已占 ID）中选取最优者，仍在阈值内则分配；超阈值则标记 unknown

**复杂度**：O(N)，不增加额外识别成本。每张脸只做一次编码比对（top_k 由一次比对结果排序得到）。

#### 前端 UI 优化

- **上传预览**：图片居中撑满上传框，竖图限高 360px 且居中，鼠标悬停右上角出现 × 关闭按钮可清除图片
- **上传框弹跳修复**：预览时保持 padding 不变，仅隐藏提示文字
- **识别结果折叠**：识别完成后的结果卡片可点击标题收起/展开
- **活动列表翻页**：固定在左侧卡片底部，不随表格行数变化而移动
- **参与人员弹窗**：加宽至 560px，内容超过 60vh 可滚动，表头 sticky 固定

